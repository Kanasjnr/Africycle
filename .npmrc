# Force legacy peer deps for all builds
legacy-peer-deps=true

# Disable optional dependencies that might cause issues
optional=false

# Disable funding and audit for faster builds
fund=false
audit=false
